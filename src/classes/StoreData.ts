class StoreData {
    constructor (public uid :string, public name :string, public location :string) {
        this.uid = uid
        this.name = name 
        this.location = location
    }
}

export default StoreData